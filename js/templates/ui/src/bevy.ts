// @generated placeholder — replaced by the real, typed client when you run:
//
//   npm run bevy:generate
//
// That command builds your Rust app with `--export-bindings` and writes the
// typed `emit` / `request` / `on` wrappers plus a `bevy` proxy here, derived
// from your #[react_message] / #[react_request] / #[react_event] types.
//
// Until then this re-exports the untyped primitives so imports resolve.
export {
  emit,
  request,
  addEventListener as on,
  removeEventListener,
} from "bevy-react";
