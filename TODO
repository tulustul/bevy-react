Release blockers:
  ☐ Path-dep versions for crates.io - `Cargo.toml [workspace.dependencies]` references `bevy-react-animations` / `bevy-react-canvas` / `bevy-react-macros` by `path` only. crates.io rejects path-only deps — each needs `version = "0.1.0"` alongside `path`, and the leaf crates must be published first (macros → canvas/animations → core).
  ☐ npm publish model - `js/package.json` `exports` ship raw `./src/*.ts` (no build output). Confirm that's intended for the esbuild-bundling consumer, or add a build step and ship compiled JS + `.d.ts`.
  ☐ Add `CHANGELOG.md` and `CONTRIBUTING.md`
  ☐ stabilize API
    ☐ Mark the public wire enums `#[non_exhaustive]` (`crates/core/src/protocol.rs:33` `Op`, `:1320` `Outbound`). Both are pub via `pub mod protocol` ("exposed for advanced use"); adding a variant is semver-breaking for any downstream exhaustive `match`. Cheap pre-publish insurance.

Correctness:
  JS-input panics (extends the archived "Panic audit", which only checked `.unwrap()`/`.expect()`, not panicking library calls):
    ✔ `parse_grid_placement` panics the app on ordinary JS style (`crates/core/src/ui_map.rs`). `gridColumn:"0"`→`GridPlacement::start(0)`, `"span 0"`→`span(0)` panic Bevy 0.19's constructors (`try_into_grid_index` `.expect`s on 0, bevy_ui `ui_node.rs:2086`) → crashed the Update schedule. (NOTE: only ZERO panics — `"2 / 2"` is fine; the original "guard zero/equal" over-guarded.) Fixed: `try_grid_placement` tokenizes each component and any zero line/span (or unrecognized token) aborts the whole placement → `warn!` + `auto()` via `unknown_keyword`, never a partial placement like `"2 / 0"`→`start(2)`; also upgraded the previously-silent garbage fallback to a warn and added `"auto / 3"`→`end(3)` support. Tests: `grid_placement_zero_falls_back_to_auto` covers all six zero forms + garbage. @done(26-07-01)
    ✔ `AnchorScaling` min/max reach `f32::clamp` unchecked (`crates/core/src/anchor.rs`). Swapped bounds or a NaN bound panicked every frame; `factor:0` with `dist==0` gave `0*inf=NaN` scale. Fixed in two layers: `AnchorScaling::sanitized()` runs once at apply time (`apply_anchor`, reconcile.rs) — non-finite field → warn + disable scaling, `min>max` → warn + swap — and the per-frame math is extracted into `distance_scale()` which resolves a NaN product to `max` (closest → largest, matching the doc'd `inf` behavior). Tests in `anchor.rs::tests`. @done(26-07-01)
    ✔ Spring `stiffness`/`damping` used unclamped while `mass` is clamped (`crates/animations/src/lib.rs`). `withSpring({stiffness:-1})` (or `0`, or NaN) diverged to NaN / never settled → runner never dropped. Fixed: both now clamp to `1e-4` exactly like `mass` (`f32::max` maps NaN to the floor too). Test: `spring_survives_hostile_params` steps hostile springs to settlement asserting finiteness. @done(26-07-01)
  Behavior:
    ☐ Opacity overwrites base alpha instead of multiplying (`crates/animations/src/lib.rs:288`). Code does `s.alpha = alpha` but `protocol.rs:128` documents Opacity as "Multiplies color alpha"; a base `rgba(255,0,0,0.4)` with animated opacity `1.0` renders fully opaque. Fix to `s.alpha *= alpha` (or correct the doc if overwrite is intended).
    ☐ Pending request promises never rejected on reload/shutdown (`js/src/bridge.ts:476`). `case "reload": return;` and `msg == null` break exit `runEventLoop` without draining `pendingRequests`; since the isolate persists across hot reload, an in-flight `await bevy.*.get()` at reload time hangs forever. Reject all pending entries before returning/breaking.
    ☐ Dropped `Responder` hangs the JS promise forever (`crates/core/src/request.rs:65`). `Responder<R>` has no `Drop` impl that rejects: a handler that early-returns without responding (e.g. responds only on its success branch) or loses a deferred responder to a despawn never emits `Outbound::Response`, and `pendingRequests` (`js/src/bridge.ts:238`) has no timeout — `await bevy.foo()` blocks permanently. Unknown-name (`request.rs:262`) and malformed-payload (`:237`) paths both reject; this is the one Rust-side failure mode that hangs. Fix: `impl Drop for Responder` → send a `ResponseResult::Err` if `!done`.
    ✔ `onClick` fires on mouse-down, not press+release (`crates/core/src/reconcile.rs:1004`). Emitted on the `Changed<Interaction>` → `Pressed` transition with no release-side check, so press → drag off → release still clicks — diverges from the DOM semantics React authors expect and makes drag-away-to-cancel impossible. Fixed: main-window clicks migrated to bevy_picking `Pointer<Click>` (press+release on the same node; drag-away cancels). `onClick` stays primary-button-only (DOM `click` parity, explicit user decision — no button on the event); the `onPointer*` events were generalized to any of left/middle/right and carry `UiEvent.button` in DOM numbering (surfaces included: `drive_surface_pointer` forwards all three with per-button owed-release tracking; `pressStyle` stays primary-only on both paths). Required companion: `focusPolicy` is mirrored into `Pickable.should_block_lower` (the picking backend ignores `FocusPolicy` and blocks when `Pickable` is absent), which as a bonus makes `focusPolicy` work on `<surface>` subtrees for the first time; surface hover collectors moved from `Over`/`Out`+climb to ancestor-aware boundary-only `Enter`/`Leave`. Tests: `picking_click_fires_once_primary_only`, `surface_pointer_clicks_are_not_main_clicks`, `serializes_pointer_event_with_button`, extended `focus_policy_defaults_block_button_pass_node`. @done(26-07-02)
    ☐ `ariaLabel` is a silent no-op on `<button>`/`<node>`/`<image>` (`crates/core/src/reconcile.rs:728`). Only `<editableText>` gets an `AccessibilityNode` (`:310`), and the label-writing branch (`:519`) is gated on `editable_inputs`, so an icon-only `<button ariaLabel="Close">` announces nothing to screen readers. Insert an `AccessibilityNode` with a proper role for buttons/images and apply `ariaLabel` generically.
    ☐ `last_good_app` promoted before the refresh render is validated (`crates/core/src/js_thread.rs:316`). `last_good_app = new_code` the instant `execute_script("[app-update]")` returns Ok, but a bundle whose top-level IIFE runs clean can still fail its Fast-Refresh render (the error is swallowed by mount.ts's try/catch and the reconciler's `onUncaughtError`) — the UI survives, but the render-broken bundle becomes the `[app-restore]` fallback, defeating "known-good". Also untested: `crates/core/tests/hot_reload.rs` covers only the happy path — no test for `[app-restore]` or the top-level-Ok-but-render-throws case.
  Lower severity:
    ☐ Keyboard non-Character keys stringified via Rust `Debug` (`crates/core/src/keyboard.rs:66`) — Space becomes `"Space"` where the web `KeyboardEvent.key` contract is `" "`.
    ☐ ts-rs declaration dedup keys by type *name*, not `TypeId` (`crates/core/src/ts_codegen.rs:344`) — two distinct Rust types ts-rs names identically silently overwrite each other in generated `bevy.ts`.
    ☐ Event dispatch iterates the live listener `Set` (`js/src/bridge.ts:499`). Self-unsubscribe (the one-shot pattern) is safe, but a listener that removes a *different* not-yet-visited listener mid-dispatch skips it, and one added mid-dispatch fires within the same event. Snapshot before iterating (`for (const cb of [...set])`).

Performance:
  Quick wins (highest value first):
    ✔ `HANDLER_PROP_KEYS` is stale — 5 of 11 handlers (`js/src/renderer.ts:49`). Missing `onPointerEnter`/`onPointerLeave`/`onSelect`/`onFocus`/`onBlur`/`onScroll` fell into the `Object.is` branch, so their ever-changing closures made `bevyPropsChanged` always true → a full `update` op + re-serialize every parent re-render for nodes using them. Fixed: the set is now derived (`bridge.ts` exports `HANDLER_PROP_KEYS = new Set(Object.keys(HANDLER_KINDS))`; `renderer.ts` imports it) so it can't drift. NOTE: the 4-places-to-edit drift class itself is still open (Cleanup item below — serializeProps/`SerializedProps` remain hand-listed). @done(26-07-01)
    ☐ Settled opacity rewritten every frame (`crates/core/src/transition.rs:546`). When `style.opacity` is set but not transitioning, the block still does `tc.0 = tc.0.with_alpha(...)` (+bg/image) unconditionally, tripping `Changed<TextColor>`/`Changed<BackgroundColor>` forever → per-frame re-extraction. Skip the write when unchanged (set-if-neq, as `anchor.rs::set_visibility` does).
    ☐ `apply_style` bare-inserts paint components, marking them `Changed` when equal (`crates/core/src/ui_map.rs`). `FocusPolicy` is inserted unconditionally, and `BackgroundColor`/`BorderColor`/`Outline`/`BoxShadow`/gradients/`ZIndex`/`GlobalZIndex` are re-inserted whenever their group runs — Bevy's `insert` always marks `Changed` regardless of equality. NOTE (26-07-02): deeper-perf #1 shrank the blast radius a lot — a delta `Op::Update` now skips untouched groups entirely (`apply_style_masked`), so this cost remains only where a group IS dirty but resolves to an equal value, on create, and on every hover/press flip (`apply_interaction_styles` → full `apply_style`, which is now the main payer). Mirror the `set_node_if_changed`/`set_if_neq` treatment `Node` already gets.
    ☐ Reuse the ops buffer in `apply_js_ops` (`crates/core/src/reconcile.rs:126`). `let mut ops = Vec::new(); … ops.extend(batch)` allocates + copies every frame on the hottest path. Move the `Vec<Op>` into `JsBridge` and `std::mem::take` it out (drain+clear) to keep capacity across frames — `mem::take` hands back an owned Vec so the "don't hold the borrow" comment still holds.
    ☐ Gate the flush instrumentation (`js/src/bridge.ts:200`). `flush()` runs every commit (~60fps) yet unconditionally does two `nowMs()` calls + a fresh object alloc + a global write just so a benchmark host might read `__bevyReactFlush`. Wrap in a dev/flag check so production commits skip it. NOTE: the stress bench reads it (`examples/stress/ui/src/App.tsx:139`) — gate, don't delete. Also drop the two commented-out `console.log` scaffolding lines there.
    ☐ `on_focus_gained`/`on_focus_lost`: `ResMut<JsBridge>` → `Res<JsBridge>` (`crates/core/src/reconcile.rs:1162`). Neither mutates the bridge, so `Res` removes needless scheduler serialization. Free, but focus events are rare — cleanup, not measurable speed.
    ☐ `PointerCapture` written unconditionally every frame (`crates/core/src/reconcile.rs:1354`). `capture.dragging`/`capture.over_ui` are assigned each tick (plus a full `Interaction` scan) so the resource is `Changed` even when idle. No `Changed<PointerCapture>` consumer exists today, so the cost is just the scan + spurious tick — low; `set_if_neq` + deriving `over_ui` from `Changed<Interaction>` transitions is a one-line hardening.
  Deeper / architectural (ranked by payoff; best payoff-to-risk: #2 and #3, #1 is the biggest win but riskiest):
    ✔ 1. Partial/diffed `Op::Update` — kill update amplification. `Op::Update` now carries only changed props + `unset`/`styleUnset` name lists (UNSET vs UNCHANGED disambiguated); deltas are the ONLY update form — the transitional `diff:false` legacy full-replace path was dropped same-day (no backward compat needed pre-release; a pre-delta bundle must be rebuilt). JS: `buildUpdateOp` (`bridge.ts`) diffs old/new in `commitUpdate` — handlers by presence, `style` field-by-field, others structurally (`valuesEqual`, depth-capped) — so an inline-but-identical style now emits NO op at all (demo-switch idempotent re-render: 205 ops → 0; switch batch 242 → 53). Rust: `JsBridge::props_cache` (Boxed — `Props` is KB-scale, moves matter) retains last-applied props; `Props::merge_delta` merges the delta and derives a `PropsDirty`/`StyleDirty` group mask from the single `style_fields!` table (`protocol.rs` — also absorbed `overlay_style`'s drift TODO with a compile-time completeness guard + wire-name test); `apply_style_masked` + gated reconcile branches only rebuild dirty groups (Node only on layout fields, per-paint-component bits, TEXT gates the O(children) span re-propagation, `StyleVariants` only on style-prop changes — also killed the spurious `Changed<StyleVariants>` double-restyle per update). Event-like props (`value`/`selection*`/`scrollTop/Left`/`draw`) split into `UpdateEvents`, never cached/replayed — controlled values now only push when the prop actually changed. Bench (table-ops p50 vs baseline, benchmark_results/{baseline,after3}-partial-update.json): updateEvery2ndBackgroundColor total −12% (flush −21%, translate −14%, command −84%); updateEvery2ndText −4%; clear +6% (10k cache-entry drops — inherent). Tests: `protocol::tests` merge-semantics suite, reconcile change-tick tests (`delta_update_skips_untouched_groups` etc.), JS rig `js/test/bridge.test.mjs` (`npm test -w bevy-react`, wired into CI). NOTE: demo_switch's liveness probe now clicks the state-toggling button — op silence on an idempotent re-render is the optimization, not a dead runtime. @done(26-07-02)
    ✔ 2. Decode enum-like style fields into real enums at the serde boundary. All 24 string-parsed `Style` fields (18 keyword + 6 grid) now decode **directly into `bevy_ui`/`bevy_text` types** via field-level `#[serde(deserialize_with)]` (sidesteps the orphan rule) — protocol.rs's bevy-free constraint was deliberately dropped (module docs updated) instead of building a mirror-enum layer, which also removes the mirror-default-must-match-bevy-default sync hazard. `keyword_fields!`/`grid_fields!` macros in protocol.rs generate the deserializers (warn + bevy-default fallback, `null`→None, non-string still hard-errors like `Length`); grid tokenizers (`split_tracks`/`single_track`/`repeated_track`/`try_grid_placement` incl. the zero-panic guard) moved from ui_map into protocol. `node_from_style` is now a plain field copy; deleted `unknown_keyword` + the 13 keyword fns + `font_weight`/`justify`/`linebreak` from ui_map; `apply_style_masked` FOCUS_POLICY + reconcile's `apply_button_focus_default` read the typed field. Behavior deltas (log-only): unknown keywords warn once at decode instead of per-apply; `focusPolicy` gains a warn on unknown values (still Pass). NOTE: the earlier "regenerate `bevy.ts`" claim was stale — `Style` is not ts-rs-exported (only messages/requests/events are); no JS/TS change needed, wire format unchanged. Tests: decode/fallback/grid/zero-guard suites moved to `protocol::tests` (`keyword_fields_decode_to_bevy_enums`, `unknown_enum_keywords_fall_back_to_default`, `font_weight_keywords_and_numeric`, `grid_templates_and_placement_decode`, `grid_placement_zero_falls_back_to_auto`); ui_map keeps thin copy-through tests; roundtrip (real serde_v8) green. @done(26-07-02)
    ☐ 3. Batch the Bevy→JS event channel (`crates/core/src/js_thread.rs:118`). `op_next_event` resolves ONE `Outbound` per call → one Promise + one channel recv + one `runEventLoop` turn per event; under floods (per-frame events, pointer-move streams, request bursts) that's a promise per event. Add `op_next_events()` that drains up to N into a `Vec<Outbound>`; JS wraps the batch in one `flushSync`. Ties to the "event flood / backpressure" benchmark item and the unbounded-channel debt.
    ☐ 4. Single-deserialize `op_emit`/`op_request` (`crates/core/src/js_thread.rs:104`). `op_request` takes `#[serde] serde_json::Value`, ships it in `RawRequest`, then `request::dispatch` re-deserializes `Value→T` — two allocs + reparse per message vs `op_flush`'s direct v8→`Vec<Op>`. Op can't know `T` statically (name is runtime); fix is registry-driven deserialize-once, or keep raw bytes instead of a `Value` tree. Only matters if emit/request run per-frame (e.g. a slider emitting each frame).
    ☐ 5. Memoize `serializeProps` on unchanged style refs (`js/src/bridge.ts`). Every changed node rebuilds `style`/`animated`/`anchor` objects from scratch each commit; since styles are meant to be hoisted to stable module consts, cache the serialized form keyed by input-object identity and skip re-serialization when the ref is unchanged. Pure JS-side.
    ☐ 6. Canvas: reuse the finished path across fill+stroke (`crates/canvas/src/lib.rs:242`). `path.clone().finish()` per `Fill`/`Stroke` and `Paint` rebuilt per color cmd — a `… Fill, Stroke` pair clones the full point/verb buffer twice. Finish once into a reusable `Path`, reuse across the pair. Lowest difficulty of this tier.

Leaks:
  ☐ `FilterMaterialCache` unbounded, no eviction (`crates/core/src/filter.rs:112`). A varying filter (`filter: blur(${n}px)` from a slider) mints a new `Handle<FilterMaterial>` per unique `FilterKey`, never removed → `Assets<FilterMaterial>` grows for the app's lifetime. Add LRU/refcount eviction.
  ☐ `useSharedValue` never frees its Bevy-side shared value on unmount (`js/src/animated.ts:125`). A virtualized list mounting/unmounting rows leaks ids that keep being driven each frame until the next full reset. (Verified 26-07-01: stale-value *reuse* after reset can't happen — `reset()` sends `AnimationCommand::Clear`, `bridge.ts:175` — so this is accumulation-only.)

Cleanup / docs:
  ✔ Register/dedup logic copy-pasted across `message.rs`/`request.rs`/`event.rs` and the three attribute macros; `serializeProps` hand-duplicated its handler blocks. Fixed: (1) the three registries' identical TypeId-dedup dance now lives in one `registry::register_entry` helper (a `NamedEntry` trait exposes each registration's `type_id`; the registries stay three distinct resources since ts_codegen fetches them by concrete type). (2) The macros share `try_parse_name_arg`/`parse_name_only_attr` (name-arg parsing; `react_request` chains its `response` arm) and `payload_parts` (ident/generics/lower_first name defaulting) — each macro keeps its own `quote!` body. (3) The `serializeProps` half was found ALREADY collapsed (single `HANDLER_PROP_KEYS.has(key)` branch, `HANDLER_PROP_KEYS` derived from `HANDLER_KINDS`); the last hand-listed spot was `SerializedProps`, whose 12 `onX?: boolean` fields are now a mapped type (`extends Partial<Record<HandlerPropKey, boolean>>`, keys from `HANDLER_KINDS` via `as const`). Adding a handler prop is now: one `HANDLER_KINDS` entry + the mirrored Rust `Props` flag — the irreducible cross-language pair. @done(26-07-02)
  ✔ Merge `raw_spans`/`text_spans` into one map (`crates/core/src/bridge.rs`). Fixed: one `HashMap<NodeId, SpanKind>` (`spans`, enum `RawInherited | InlineStyled`) replaces the two lock-step `HashSet`s — `UpdateText` routing checks `contains_key`, style inheritance checks `== RawInherited`, and the paired insert/clear/remove sites collapse to single calls, so the old superset invariant is unrepresentable-when-violated. Guarded by the existing `update_text_on_inline_span_keeps_textspan` + `text_delta_gates_span_repropagation` tests (unchanged, green). @done(26-07-02)
  ✔ Stale `export_react_typescript` rustdoc (`crates/core/src/message.rs`). Fixed: rewritten around the current convention — the full `bevy.ts` surface (all three maps + typed wrappers + the `bevy` proxy), a single shared `register_bindings(app)` fn called by both the real app and a `--export-bindings` exporter that returns before `app.run()` (pointing at `examples/demos/main.rs` / `npm run bevy:generate`), and the CI regenerate-then-diff guarantee; the resolved duplicate-registration `TODO(review)` block is deleted. Also fixed the same-class stale `generated.ts` mentions in `examples/demos/main.rs` and `ts_codegen.rs`'s module doc, and dropped CLAUDE.md's stale note about `.prettierignore` (it already ignores `**/bevy.ts`). @done(26-07-02)
  ✔ Stale comment in `js/src/animated.ts` — "A hot reload spins up a fresh isolate, so this resets to 1 naturally" predated the persistent-isolate model. Fixed: the comment now states the isolate persists across hot reloads (this module lives in the never-re-executed vendor bundle), so `nextSharedId` is monotonic — fine since ids only need uniqueness — and `reset()` sends `AnimationCommand::Clear` before the re-render re-declares values under fresh ids. Code unchanged. @done(26-07-02)

Features:
  Interaction & events:
    ✔ Raw `onWheel` — wheel input only drove `overflow: scroll` containers; a non-scroll node (e.g. a zoomable `<canvas>` map) couldn't receive wheel deltas. Added a per-node `onWheel` handler (on `node`/`button`/`canvas`/`portal`; `<surface>` deferred — needs the in-world virtual pointer) delivering the RAW frame delta (`deltaX`/`deltaY` + `deltaMode` `"line"|"pixel"`, DOM `WheelEvent`-style, no `scrollStep` scaling) plus the cursor position (`x`/`y` normalized + `clientX`/`clientY`). Target selection is a GEOMETRIC hit-test over the `UiStack` (topmost `WheelListener` node whose rect contains the cursor), mirroring `apply_scroll` — deliberately NOT the `Interaction`/`RelativeCursorPosition` model the `onPointer*` handlers use, which would force `apply_wheel_listener` to fight `apply_pointer_handlers` over `RelativeCursorPosition` insert/remove for an `onWheel`-only node. A delivered wheel sets `PointerCapture::over_ui` so a world zoom-camera ignores it. CRUCIALLY it also BLOCKS ancestor scroll (DOM `preventDefault` semantics): `apply_scroll` now walks the same `UiStack` topmost-first and, when the topmost node under the cursor is a `WheelListener`, RETURNS without scrolling — so an `onWheel` box inside a scrollable page swallows the wheel instead of also scrolling the page. (`over_ui` alone only gates WORLD systems, not `apply_scroll`, which hit-tests scroll containers independently — that was the bug: the box's wheel scrolled the demo page too.) Known edge case (noted, not chased): a scroll container stacked ABOVE a listener still lets `collect_wheel_events` fire the underneath listener; fully-correct precedence would need a single unified walk (blocked by `apply_scroll`'s test-friendly bevy-free signature). Scaffolding: `Props::on_wheel` + `PropsDirty::wheel` + `merge_bool!`/unset arms (`protocol.rs`), `UiEvent.delta_x/delta_y/delta_mode` wire fields, `WheelListener` marker (`bridge.rs`), `apply_wheel_listener` (`reconcile.rs`, one create site + `dirty.wheel` update branch), `collect_wheel_events` (`scroll.rs`), registration in a `PointerCaptureSet` sub-tuple after `collect_pointer_events` (the `Update` tuple is at the 20-arity cap). JS: one `HANDLER_KINDS` entry (`onWheel: "wheel"`) + `UiEvent` delta fields (`bridge.ts`), `WheelEventData` + `onWheel` props (`jsx.d.ts`). No `bevy.ts` regen (per-node flags aren't ts-rs-exported). Demo: `WheelExample` (zoom box) in `MouseDemo.tsx`. Tests: `collect_wheel_events` suite (`scroll.rs`) + `merge_delta_wheel_flag` (`protocol.rs`). @done(26-07-02)
    ☐ `cursor` style prop — the window `CursorIcon` is never driven; a `<button>` can't show a pointer hand on hover, `<editableText>` no I-beam. Hover state is already tracked (`HoverState`), so a per-node prop → set-window-cursor system is straightforward.
    ☐ Per-node `onKeyDown`/`onKeyUp` — keyboard is global-only (`bevy.on("keyDown")`, `crates/core/src/keyboard.rs`); scoped key handling (listbox arrow-nav, dialog Esc) forces every widget to subscribe globally and disambiguate by hand. Needs the focus-target infra from the item below.
    ☐ Generic focus event callbacks - `onFocus`/`onBlur` only fire for `<editableText>` today; arbitrary `<node>`/`<button>` aren't focusable (would need `TabIndex`/focus-target infra + keyboard nav to fire `FocusGained`/`FocusLost`).
  Assets & feedback:
    ☐ `<image>` `onLoad`/`onError` — `image_node` (`crates/core/src/ui_map.rs:930`) fire-and-forgets `assets.load(path)`; a bad path renders nothing forever and React is never told, so spinner/fallback UI is impossible. Watch the asset load state and emit per-node events.
  API:
    ☐ Request timeout / cancellation — `request()` (`js/src/bridge.ts:236`) accepts no `AbortSignal`/timeout, so a handler that legitimately takes forever can't be abandoned by the caller. Complements the `Responder`-drop rejection fix (Correctness), which covers the handler-side failure.
  Layout (niche):
    ☐ `direction` (Ltr/Rtl) - no RTL layout.
    ☐ `overflow_clip_margin` (`VisualBox` + margin) and `OverrideClip` (per-node clip opt-out).

Benchmarks and stress tests:
  Framing: the reconciler IS `react-reconciler` (same Fiber engine as web React), so split
  results into (a) apples-to-apples reconcile work — run the SAME component tree through a
  react-dom host and the Bevy host, the delta is pure host/bridge overhead — and (b)
  apples-to-oranges paint (bevy_ui/GPU vs DOM). The headline artifact is a "reconciler is
  identical, here's the host overhead" chart.
  Library-specific:
    ☐ Op-flush hot path (criterion, headless): `serde_v8` deserialize + `apply_js_ops`
      (`crates/core/src/reconcile.rs`) + `ui_map` vs `Vec<Op>` length (10 → 100k). Proves
      the "no JSON strings on the hot path" claim.
    ☐ Mount cost vs tree shape: deep (10k-deep) vs wide (10k siblings) vs balanced.
    ☐ Update amplification: change one prop deep in an N-node tree → assert O(1) ops, not
      O(N). Guards against reconciler/`Op::Update` regressions (relates to the full-`Node`
      rebuild debt above).
    ☐ Keyed-list churn: swap / insert / remove / reorder at scale.
    ☐ End-to-end event round-trip latency (p50/p99 under load): click → `uiEvent` →
      handler → setState → flush → applied. Extend `crates/core/tests/roundtrip.rs` with
      timing. Expect ~1 frame (Bevy→JS is async via tokio mpsc → `op_next_event`).
    ☐ Bevy→React event flood / backpressure: produce events faster than React commits;
      watch the channel grow (ties to the unbounded-channels debt above).
    ☐ Request/response under concurrency: many in-flight `op_request` correlations; verify
      none hang on malformed payloads.
    ☐ Frame-budget interaction (HIGHEST VALUE FIRST): `op_flush` is sync on the Bevy main
      thread and shares the 16.6ms budget with the 3D sim. Measure dropped frames under a
      large flush. Specifically: do world-anchored elements (declared once by React,
      positioned every frame by a Bevy system — NO per-frame bridge traffic) trigger a
      bevy_ui RELAYOUT on position change, or just transform/extract? If transform-only, N
      anchored badges scale almost free; if relayout, that's the cost to fix.
    ☐ Animations engine: N concurrent animations/frame — confirm values are driven
      Rust-side, not round-tripped to JS each frame.
    ☐ Canvas rasterizer: `DrawCmd` throughput/frame.
    ☐ Hot-reload latency + steady-state memory across many reloads (isolate stays alive).
    ☐ Leak/steady-state: frame-time stability; entity count across repeated `selectScene`
      switches (verify `DespawnOnExit` reclaims).
  Cross-framework comparison (port to bevy-react + a vanilla react-dom impl):
    ☐ js-framework-benchmark (krausest) operation set — the industry-standard table:
      create 1k/10k rows, update every 10th, swap, select, remove, clear, append.
    ☐ uibench (localvoid/uibench) — clean reconciler-only comparison, minimal paint.
    ☐ DBMonster / Sierpinski — sustained high-frequency / deep-recursive update cases.
  Expectation (to validate): reconcile ≈ web & faster than RN (Hermes); commit boundary
  faster than DOM mutation, ≈ RN Fabric; layout slower than Blink at high node counts
  (the main risk); world-anchored UI AHEAD of web (web needs per-frame rAF + style writes,
  we do it in Rust/GPU with zero JS).

Verified non-issues (26-07-01 review — don't re-chase):
  Top-level `color`/`fontSize` props are NOT silently dropped — `protocol::Props` has both (`protocol.rs:425`/`:429`, camelCase rename).
  `select_byte_range` can't panic on bad JS selection offsets — parley 0.9 no-ops unless both indices are char boundaries (`editor.rs:749`), and anchor/focus accept any order.
  Unknown request names DO reject the JS promise (`request.rs:262`); malformed payloads too (`:237`).
  Stale shared-value reuse after reset is impossible — `reset()` sends `AnimationCommand::Clear` (`bridge.ts:175` → `animations/lib.rs:178`); only the unmount accumulation leak (tracked above) remains.
  Bundle-watch `!=` mtime compare (`host/native.rs:88`) is fine on ext4 (ns resolution); only coarse filesystems (FAT ~2s) could ever miss a rebuild.

Archive:
  Release blockers:
    ✔ add docs.rs - `[package.metadata.docs.rs]` (all-features + `--cfg docsrs`) on all six lib crates, `#![cfg_attr(docsrs, feature(doc_auto_cfg))]` in each `lib.rs` for cfg labels, and a docs.rs badge in `README.md`. Documents the default native target (rusty_v8 skips its V8 download when `DOCS_RS` is set, so no network needed); the wasm web-host module is left undocumented for now (would need `wasm32-unknown-unknown` in `targets` + the `getrandom_backend` rustflag, risking heavy-Bevy build timeouts). @done(26-06-30 16:44)
  CI / correctness:
    ✔ Re-enable Rust bindings-drift check + test suite in `.github/workflows/ci.yml` (was commented out in `a131205` because linking OOM'd the runner). Fixed the OOM with the `mold` linker (apt) + `CARGO_PROFILE_DEV_DEBUG=0`, applied non-invasively via `mold -run <cmd>` on the `Test`/`Regenerate TS bindings` steps. Also corrected two stale names baked into the dead block: `gen:bindings`→`bevy:generate`, `generated.ts`→`bevy.ts`. @done(26-06-30)
    ✔ Failing JS nav tests (`demo_switch`, `roundtrip`) - now green; the `find_button` walk was already widened to 8 levels (`crates/core/tests/{demo_switch,roundtrip}.rs`). Verified `cargo test` passes the full suite locally with the bundle built. @done(26-06-30)
  Known debt:
    ✘ Unbounded channels / no backpressure - (`crates/core/src/plugin.rs:125`). A system that emits faster than JS drains grows the queue without bound. Bound them or document. @cancelled(26-07-01 13:05)
    ✔ Proper error handling e.g. incorrect prop/style values - no malformed style prop can throw from `op_flush` any more (a throw there aborted the whole commit batch and wedged the reconciler mid-commit → frozen UI). Bad unit-bearing values (`Length`/`Angle`/`Time`/`FontSize`), `Rect` shorthand tokens / value-counts (`padding`/`margin`/`border`/`borderRadius`), and unknown rect/`borderColor` side keys now all warn + fall back to a default instead of erroring (`protocol` uses the neutral `tracing` facade, staying bevy-type-free). The silent enum mappers (`display`/`align*`/`justify*`/`flexDirection`/… via `unknown_keyword`), grid track parsing, and `fontWeight` now warn on unrecognized values too, matching the existing color/`fontFamily` warn+fallback pattern. NOTE remaining gap: a wrong JSON *type* for a scalar field (`width={{}}`/`width={true}`) still throws from `deserialize_any`; TS guards it (`js/src/jsx.d.ts`), so deferred. Also hardened the hot-reload path (`js_thread.rs`): a rebuilt bundle that throws on execution — a syntax error OR a runtime error like `padding: aa16` (undefined identifier) — is now REJECTED rather than applied; the loop re-runs the last working bundle (re-parking the event loop) instead of escalating to a destructive full rebuild and then `break`ing the JS thread (which used to freeze the UI permanently with no recovery). Covered by `crates/core/tests/broken_reload.rs`. Remaining: a broken *initial* (cold-start) bundle still can't start (no last-good to fall back to). @done(26-06-30 16:44)
    ☑ Panic audit on JS-derived data - audited: no production hot path uses `.expect()`/bare `.unwrap()` on JS-derived data (all use `.unwrap_or*`). The flagged `.expect()`s are all test-only: `crates/core/src/anchor.rs:214`/`:217` are inside `#[cfg(test)] mod tests` (`props_deserialize_anchor`), and the old `reconcile.rs:674 .expect("valid text props")` is now `reconcile.rs:1523` in a test helper. (The request path already rejects on bad input; serde-boundary failures no longer panic — see "Proper error handling" above.) NOTE: superseded by the "JS-input panics" section above — the audit missed panicking *library* calls (`GridPlacement`, `f32::clamp`).
    ✔ Full `Node` rebuild on every `Op::Update` - `apply_style` (`crates/core/src/ui_map.rs`) now updates `Node` in place via a queued `EntityCommand` (`set_node_if_changed`) that `set_if_neq`s the freshly-built node, so a paint-only change (e.g. `backgroundColor`) no longer marks `Node` changed and no longer forces a `bevy_ui` relayout of the subtree. Centralized at the single funnel, so `Op::Update` AND the hover/press restyle systems (`apply_interaction_styles`/`apply_surface_interaction_styles`) all benefit. Measured via the stress `updateEvery2ndBackgroundColor` op (relayout gone) vs `updateEvery2ndText` (relayout retained, as it must). Remaining: the hand-maintained `Style`→`Node` mirror list (`node_from_style`) still must match `Node` field-for-field — easy to desync, but that's a maintenance concern, not perf (deeper-perf #2 above dissolves it). @done(26-07-01)
    ✔ Descendant leak on single-node removal - (`crates/core/src/reconcile.rs:230`). @done(26-07-01 14:36)
    ✔ Surface JS runtime errors - the prelude now installs a `console` that forwards every `console.*` call (and the runtime's own error handlers) to the Bevy log via `op_log`, tagged `target: "js"` with the tracing level distinguishing `log`/`warn`/`error`. @done(26-06-30 16:44)
    ✘ Single-root / single-isolate - limitation (`crates/core/src/plugin.rs:312`) — one UI surface per app. Document explicitly. @cancelled(26-07-01 14:41)
  Features:
    ✔ `<editableText>` completeness (IME, clipboard, selection) + accessibility. IME and clipboard (Ctrl+C/V/X) already worked via Bevy's `EditableTextInputPlugin`; bridged the rest to React: `onSelect` + controlled `selectionStart`/`selectionEnd` (UTF-8 byte offsets via parley's `select_byte_range`), `onFocus`/`onBlur` + `autofocus`, a `composing` flag on change/select, and an `AccessibilityNode` (`TextInput`/`MultilineTextInput`) with `ariaLabel` + live value sync. Deferred: `placeholder` (overlay text entity), `readOnly` (edit-blocking), full accesskit text-selection (needs parley's run subtree). @done(26-06-27)
    ✔ named colors - CSS named colors (basic + extended palettes) + `transparent`, via the shared `bevy_react_canvas::parse_css_color`. @done(26-06-25)
    ✔ proper units support in styles - colors gained named/`rgb()`/`hsl()`/`hwb()`/`oklab()`/`oklch()` notation (warn+magenta on a typo, no longer silent white); new `Angle` (deg/rad/turn/grad, bare number = degrees — `transform.rotate` now degrees, was radians) and `Time` (ms/s, bare number = ms, ms→s conversion moved from JS into Rust) wire types decode at the serde boundary like `Length`; `lineHeight`/`letterSpacing` accept CSS unit strings. Lengths were already a complete `Val` mirror. NOTE: `Length`/`Angle`/`Time` no longer fail-fast on a bad string — handled under "Proper error handling" above (they warn + fall back to default at the serde boundary via the `tracing` facade). @done(26-06-25)
    bevy_ui 0.19 coverage (audited against bevy_ui 0.19.0; layout/flex/grid/text-basics are complete):
      ✔ Gradients - entirely absent. `LinearGradient` / `RadialGradient` / `ConicGradient` + `BackgroundGradient` / `BorderGradient` (`bevy_ui::gradients`). No gradient field exists in `protocol::Style`/`Props`. Headline 0.19 feature. @done(26-06-25 21:32)
      ✔ Image 9-slice / tiled scaling - `imageMode` now accepts `{type:"sliced", border, centerScaleMode, sidesScaleMode, maxCornerScale}` (`NodeImageMode::Sliced(TextureSlicer)`) and `{type:"tiled", tileX, tileY, stretchValue}`, in addition to `auto | stretch`. @done(26-06-25)
      ✔ Image source-rect / atlas - `<image>` now exposes `sourceRect` ({x,y,width,height} → `ImageNode.rect`), `atlas` (uniform sprite-sheet grid → cached `TextureAtlasLayout` + `texture_atlas`, with a per-grid `AtlasLayoutCache` so index-only animation doesn't leak a layout/commit), and `visualBox` ("content"/"padding"/"border" → `ImageNode.visual_box`). @done(26-06-28)
      ✔ `GlobalZIndex` - `globalZIndex` style prop now maps to `bevy_ui::GlobalZIndex` (mirrors the `zIndex`→`ZIndex` path in `protocol::Style`/`ui_map`). Demo: Styling → "Z-Index" toggles `none`/`zIndex:99`/`globalZIndex:99` on an overhanging popover, proving local z can't escape the parent stacking context. @done(26-06-28)
      ✔ Per-side `borderColor` - `borderColor` now accepts a `{top,right,bottom,left}` object of colors (omitted sides → transparent) in addition to the uniform string form; maps to `bevy_ui::BorderColor`'s per-side fields. New `protocol::BorderColorSpec` wire type (string or object). Demo: Styling → "Borders" shows a four-color box. @done(26-06-29)
      ✔ Text typography - `lineHeight`, `letterSpacing`, `TextShadow`, and line-break/wrap control (`TextLayout` linebreak) all unexposed. @done(26-06-25 19:46)
      ✔ Multiple stacked box shadows - `boxShadow` maps a single `ShadowStyle`; `bevy_ui::BoxShadow` is `Vec<ShadowStyle>`. @done(26-06-29 11:43)
      ✔ `FocusPolicy` (Block/Pass) - `focusPolicy` style prop maps to `bevy::ui::FocusPolicy` (`"block"`→`Block`, `"pass"`→`Pass`); `apply_style` always inserts (never removes — removal would silently fall back to `Block`). Element-dependent default: a `<button>` blocks (captures the click, mirroring bevy_ui's native `Button`; re-asserted in the reconciler's create + update paths via `apply_button_focus_default`), a `<node>`/container passes — so a `<text>` or wrapper never swallows clicks meant for what's behind/around it. Also fixed: `onClick` now attaches an `Interaction` so a bare `<node onClick>` (no hover/press/pointer prop) is actually clickable. Demo: Styling → "Focus Policy" toggles a front box between pass/block over a clickable back box. @done(26-06-29)
      ✔ Controlled `ScrollPosition` - scroll input is handled (`scroll.rs`), but offset can't be set/read from React. @done(26-06-29 14:01)
      ✔ AlignItems/AlignContent `Start`/`End` vs `FlexStart`/`FlexEnd` - `start`/`end` now map to the physical `Start`/`End` variants (writing-direction relative) while `flexStart`/`flexEnd` keep mapping to the flow-relative `FlexStart`/`FlexEnd`; they diverge in grid and reversed-flex containers. Applies to `alignItems`/`alignSelf`/`alignContent`/`justifyContent` (`ui_map.rs`); TS unions already listed both. Demo: Layout → Flex adds `rowReverse` + `start` options so the split is visible. @done(26-06-29)
      ✔ Responsive transform translate - `transform.translateX`/`translateY` now take the full `Length` surface (number = px, or a unit string like `"50%"`/`"10vw"`) instead of px-only, mapping to `UiTransform.translation` (`Val2`). Threaded through static (`ui_map`), transition (translate channels migrated from scalar `Channel` to `LengthChannel`, so a percent translate eases via `lerp_length` and snaps on a unit change — same semantics as `width`/`height`), and animated (`build_ui_transform` now takes `Option<Val>`; the animation path stays px via `Val::Px`, since the leaf `animations` crate can't see `core`'s `Length`). Demo: Styling → Transform adds a `translateX:"50%"` toggle. @done(26-06-29)
      ✔ Hover event callbacks - `onPointerEnter`/`onPointerLeave` on `node`/`button`/`canvas`/`portal`/`surface`, fired once per hover-boundary crossing (main window from `Interaction` transitions gated by a `HoverState`; `<surface>` from the virtual pointer's `Pointer<Over>`/`Pointer<Out>`). Carry the same `PointerEventData` (normalized `x`/`y` + `clientX`/`clientY`) as the other pointer handlers. @done(26-07-01)
      ✘ Native `ViewportNode` (render-camera-into-UI) - partly overlaps custom `<portal>`/`<surface>`, but the native widget isn't wired. @cancelled(26-06-29 00:23)
      ✘ Widget interaction-state markers - `Checkable`/`Checked`/`Selectable`/`Selected`, `Label`, `UiTargetCamera` (mostly internal/widget-level). @cancelled(26-06-29 11:44)
