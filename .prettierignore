node_modules
**/dist
target
package-lock.json

# Generated by bevy-react's `App::export_react_typescript` exporter — the Rust
# `#[react_message]`/`#[react_request]`/`#[react_event]` types are the source of
# truth, so prettier must not reformat (it would break the regen diff check).
**/bevy.ts
